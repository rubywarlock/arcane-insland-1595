<%= form_for(@gmenu) do |f| %>
  <% if @gmenu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gmenu.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% @gmenu.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "genmenuname", "Menu name" %><br />
    <%= f.text_field :genmenuname %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>